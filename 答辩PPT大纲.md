# 选择困难终结 — Vibe Coding 答辩 PPT 大纲

> 课程：Python 程序设计 | 讲师：李承浩
> 项目类型：课程大作业 3 — Vibe Coding
> 小组人数：3 人

---

### PPT 结构速览（11 页，5-10 分钟）

| 页码 | 内容 | 对应要求 |
|:---:|------|:---:|
| 1 | 封面 | 开场 |
| 2 | 需求设计文档 | 要求 1 |
| 3 | 系统架构（逻辑先行） | 要求 1 |
| 4 | 小组三人分工 | — |
| 5 | 系统演示 | 要求 2 |
| 6 | 最棒的 Prompt | 要求 3-展示1 |
| 7 | AI 犯错案例 ×3 | 要求 3-展示2 |
| 8 | Vibe Coding 使用反思 | 要求 4 |
| 9 | 🆕 Vibe Coding 完整规划过程（5 步） | 要求 1+4 |
| 10 | 优势/不足/自评 | 必答题 |
| 11 | 答辩分工 | — |

---

## 第 1 页：封面

- **项目名称**：选择困难终结 — 智能决策助手
- **项目类型**：课程大作业 3 · Vibe Coding（全程 AI 生成，零手写代码）
- **小组信息**：组员姓名 + 学号（3 人）
- **线上地址**：https://dariaaaa.pythonanywhere.com
- **GitHub**：https://github.com/dongyifei0502/choice-terminator

---

## 第 2 页：需求设计文档（对应答辩要求 1）

### 2.1 需求背景
- 日常选择困难症（纠结午餐选什么、购物选哪个）
- 需要一个多维度加权打分系统，用数据辅助决策
- 需要云端存储历史，支持多设备同步

### 2.2 功能需求
| 模块 | 功能 |
|------|------|
| 决策引擎 | 输入选项 → 多轮打分 → 加权计算 → 推荐结果 |
| 结果展示 | 柱状图可视化 + 得分明细表 + 推荐理由 |
| 用户系统 | 注册/登录/JWT 认证（零外部依赖） |
| 历史记录 | 云端同步 + 本地兜底 + JSON/CSV 导出 |
| 管理后台 | 问题库 CRUD + 用户封禁管理 |

### 2.3 设计文档截图
- PRD 文档关键页（需求梳理 → [PRD-选择困难终结.md](PRD-选择困难终结.md)）
- UI 设计规范（配色/字体/组件 → [UI设计规范.md](UI设计规范.md)）
- 开发计划依赖关系图（12 步骤 → [开发计划.md](开发计划.md)）

---

## 第 3 页：系统架构（逻辑先行 — 对应 Vibe Coding 要求 1）

### 3.1 技术架构图

```
┌─────────────────────────────────────────┐
│          前端 SPA (Vanilla JS)           │
│  首页 → 问答 → 评分 → 结果 → 历史 → 管理  │
│          Chart.js / CSS3 / HTML5         │
└──────────────────┬──────────────────────┘
                   │ REST API
┌──────────────────▼──────────────────────┐
│          Python 后端 (Flask)             │
│  auth.py │ questions.py │ history.py     │
│         SQLite (零配置数据库)              │
└─────────────────────────────────────────┘
```

### 3.2 核心逻辑=先画出来再让AI生成

```
Step 0 脚手架 ──→ Step 1 首页 ──→ Step 2 问答页 ──→ Step 3 评分引擎
                                                      │
                                     ┌─→ Step 5 图表  │
                                     ├─→ Step 6 理由  │
                                Step 4 结果页 ←────────┘
                                     │
                                Step 7 历史 → Step 8 导出
                                     │
                                Step 9 管理页
                                     │
                            阶段三 Flask 后端
                            阶段四 用户+封禁
                            阶段五 PythonAnywhere 部署
```

---

## 第 4 页：小组分工

### Vibe Coding 角色分配

| 组员 | 角色 | 负责模块 | AI 工具 |
|:---:|------|----------|:---:|
| **组员 A** | 需求 + 架构师 | PRD 撰写、逻辑梳理、12 步开发计划、提示词设计、分步分解 | Claude Code |
| **组员 B** | 后端 + 数据 | Flask 骨架、认证系统、SQLite 数据库、API 接口、权限中间件 | Claude Code |
| **组员 C** | 前端 + 部署 | SPA 页面开发、CSS 样式系统、Chart.js 图表、PythonAnywhere 云端部署 | Claude Code |

### 各阶段三人都参与

| 阶段 | A（架构） | B（后端） | C（前端） |
|------|:---:|:---:|:---:|
| 需求分析 | ★ 主导 | 参与讨论 | 参与讨论 |
| 12 步开发计划 | ★ 主导 | 提供反馈 | 提供反馈 |
| Flask 后端 | 审核 API 设计 | ★ 主导 | — |
| 前端 6 页面 | — | — | ★ 主导 |
| 四维改进 | ★ 方案设计 | 配合调整 | 配合调整 |
| 用户+封禁 | — | ★ 主导 | 前端对接 |
| PythonAnywhere | — | 协助排查 | ★ 主导 |

---

## 第 5 页：系统演示（对应答辩要求 2）

### 演示路径（5 分钟）

| 顺序 | 演示内容 | 时间 |
|:---:|------|:--:|
| 1 | 打开网站 → 首页场景选择 + 输入选项 | 30s |
| 2 | 问答页：5 道题逐题打分 + 进度条 | 1min |
| 3 | 结果页：推荐结果 + 柱状图 + 理由 + 明细 | 1min |
| 4 | 平局场景演示：🎲 随机 + 换一个 | 30s |
| 5 | 登录 (admin/admin123) → 管理问题库 | 1min |
| 6 | 用户管理：封禁/解封 | 1min |

### 演示要点
- 展示 4 种决策场景（餐饮/购物/旅行/通用）
- 展示用户注册 → 云端历史同步
- 展示管理页 Tab 切换（问题管理 / 用户管理）

---

## 第 6 页：最棒的 Prompt（对应答辩要求 3-展示1）

### Prompt 示例：Step 2 问答页

**Prompt**：
> "你现在是 Step 2：问答页的开发。依赖 Step 0（脚手架完成）和 Step 1（首页完成）。请实现：
> 1. 创建 js/router.js — 简易页面切换函数（display 显隐 + 触发目标页 init 函数）
> 2. 创建 js/services/questionService.js — selectQuestions() 按 enabled + weight 排序选题，loadQuestions() 三阶段加载（localStorage → fetch JSON → 兜底）
> 3. 创建 js/components/progressBar.js — updateProgress(current, total) 更新宽度+文字
> 4. 创建 js/components/slider.js — renderSlidersHTML() 渲染滑块（默认值3），bindSliderEvents() 实时更新分数（>=4 加 .high 类），collectScores() 收集分数对象
> 5. 修改 index.html 为多页面容器
> 6. 修改 app.js 整合路由 + 问答页逻辑
> 完成标准：首页输入3选项→进入问答→进度条+权重badge→滑块打分→前后导航→最后一题变粉色'查看结果'"

**为什么这个 Prompt 有效**：
1. **明确前置依赖** — "依赖 Step 0 和 Step 1"
2. **分模块编号** — 6 个子任务清晰独立
3. **指定函数签名** — `updateProgress(current, total)` 约束了入参类型
4. **附完成标准** — 可自行验证的验收条件
5. **上下文完整** — 单次会话能独立理解全部需求

---

## 第 7 页：AI 犯错案例（对应答辩要求 3-展示2）

### 案例 1：循环导入错误

**AI 写的代码**：
```python
# app.py
from backend.auth import auth_bp  # 导入 auth

# backend/auth.py
from app import get_db  # 又导入 app → 循环导入！
```

**发现过程**：
- 启动 `python app.py` → 报错 `ImportError: cannot import name 'auth_bp' from partially initialized module`
- 这是 Python 循环导入的经典错误

**修复 Prompt**：
> "出现了循环导入错误。auth.py 不能从 app 导入 get_db。请把 get_db 提取到独立的 backend/db.py 模块，让 app.py 和 auth.py 都从 backend.db 导入。"

### 案例 2：参数顺序错误

**AI 写的代码**：
```js
Store.setUser(data.user.username, data.user.role, data.token);
// 但函数签名是 setUser(token, username, role) ← 参数顺序反了！
```

**发现过程**：
- 登录后刷新页面 → 用户名显示的是 token 字符串
- 排查后确认是调用时参数传反了

**修复**：全局替换为 `Store.setUser(data.token, data.user.username, data.user.role)`

### 案例 3：依赖缺失

**AI 用了 flask-cors + bcrypt + pyjwt，但 PythonAnywhere 免费版无法安装**

**发现**：部署到 PythonAnywhere 后 HTTP 500，错误日志空白

**修复 Prompt**：
> "PythonAnywhere 免费版不能 pip install。请用纯标准库替代：hashlib.sha256 替代 bcrypt，hmac 替代 PyJWT，手写 after_request 替代 flask-cors。最终 requirements.txt 只需要 flask。"

---

## 第 8 页：Vibe Coding 使用反思（对应答辩要求 4）

### 好的方面

| 优势 | 说明 |
|------|------|
| 🚀 **效率极高** | 12 步前端 + 4 步后端 + 部署，1 天内完成，传统开发至少 1 周 |
| 📐 **结构清晰** | AI 严格按照开发计划的依赖关系执行，不会跳过依赖 |
| 🔧 **改起来快** | "四维改进"四个问题一次对话全部解决 |
| 🌐 **部署全程 AI** | 从 render.yaml 到 PythonAnywhere API 上传文件，全部 AI 操作 |

### 需要改进的方面

| 不足 | 说明 |
|------|------|
| ⚠️ **AI 幻觉** | AI 会使用不存在的 API（如 PythonAnywhere console API）需要人工验证 |
| 🧪 **测试依赖人工** | AI 无法自动打开浏览器测试，防御阶段的验证全靠人 |
| 📝 **Prompt 质量决定结果** | 功能描述不清时 AI 容易写出"看起来对但实际不完整"的代码 |
| 🔐 **安全需人工审查** | AI 写的认证/加密代码需要人工确认逻辑正确性 |

### 核心体会

> **Vibe Coding ≠ 不思考。恰恰相反，它要求更强的「架构思维」和「Prompt 设计能力」。**
>
> 你不需要写代码，但你必须：
> 1. 把大问题拆成小步骤（分模块、分步骤）
> 2. 每一步清晰定义输入、输出、边界条件
> 3. 亲自验证每一步的输出是否正确
> 4. 对最终代码负 100% 责任

---

## 第 9 页：Vibe Coding 完整规划过程（对应答辩要求 1+4）

### 从零到一的 5 步规划 — 全部由 AI 辅助完成，零手写代码

| 顺序 | 产出物 | 时间 | AI 做了什么 | 我做了什么 |
|:---:|------|:--:|------|------|
| ① | [需求梳理.md](需求梳理.md) | 第1轮 | 根据我描述的场景生成结构化需求文档 | 描述使用场景、目标用户、核心功能想法 |
| ② | [PRD-选择困难终结.md](PRD-选择困难终结.md) | 第2轮 | 根据需求梳理扩展为完整 PRD（数据模型、算法伪代码、验收标准） | 审核功能定义、补充边界条件、确认交互设计 |
| ③ | [开发计划.md](开发计划.md) | 第3轮 | 将 PRD 拆解为 12 步依赖关系图 + 每步的"目标/文件/完成标准" | 确认依赖关系正确、补充技术约束、调整步长 |
| ④ | [demo.html](demo.html) | 第4轮 | 生成静态设计 Demo（5 页面 + 全部组件状态 + Chart.js） | 检查视觉风格、调整配色/描边/圆角 |
| ⑤ | [UI设计规范.md](UI设计规范.md) | 第5轮 | 从 Demo 提取完整设计规范文档 | 确认规范一致、补充交互细节 |

### ① 需求梳理 → 一句话触发整个项目

**我的初始 Prompt**：
> "帮我梳理一个选择困难终结工具的需求，用户输入多个选项，系统从多个维度提问打分，最后给出推荐结果"

**AI 输出**：[需求梳理.md](需求梳理.md) — 包含项目背景、目标用户（3 类）、核心功能（10 项）、5 个页面设计、数据存储方案

**我验证了什么**：
- 功能优先级 P0/P1/P2 是否合理
- 选项上限应为 10 个而非 6 个
- 数据存储应支持云端（最初写的是纯本地）

---

### ② PRD → 从想法到产品规格

**Prompt**：
> "基于需求梳理，写一份完整的 PRD 产品需求文档，包含数据模型、算法伪代码、推荐理由生成逻辑"

**AI 输出**：[PRD-选择困难终结.md](PRD-选择困难终结.md) — 12 章完整 PRD（产品概述、功能详述、页面交互、数据模型 JSON schema、加权计算公式、边界条件表）

**我验证了什么**：
- 加权公式 `Σ(weight × score)` 手动验算
- 平局处理逻辑需要调整（最初写"取靠前的"，后来改为随机）
- 数据模型字段类型确认

---

### ③ 开发计划 → 把 PRD 变成 AI 可执行的 12 步

**Prompt**（这是最关键的一步）：
> "根据 PRD 写一份开发计划，分成 12 个独立步骤（Step 0-11），每步包含：目标、涉及文件（标注新建/修改）、具体要做什么（含代码结构）、完成标准 checklist、依赖关系图。每个步骤要足够完整，让新的 AI 会话能独立理解并执行"

**AI 输出**：[开发计划.md](开发计划.md) — 12 步依赖关系图 + 每步 4-6 个文件 + 详细 JS 代码结构 + 完成标准

**为什么这是最关键的规划步骤**：
1. 它把 3500 行代码拆成 12 个 AI 一次能消化的粒度
2. 每步的"完成标准"变成了自动验收条件
3. 每步的"涉及文件"让 AI 不会遗漏

---

### ④ Demo.html → 先看长什么样再写代码

**Prompt**：
> "根据 UI 设计方向（暖色手绘 + 粗黑描边 + 圆角扁平），生成一个包含全部 5 个页面状态的设计 Demo，含 CSS 变量系统"

**AI 输出**：[demo.html](demo.html) — 单文件包含全部页面 + CSS 变量 + Chart.js 示例

**我验证了什么**：
- 配色是否统一（米白底 + 暖黄高亮 + 粗黑边框）
- 字号/间距是否协调
- 移动端占比不合适 → 要求"去手机框 + 放大适配桌面"

---

### ⑤ UI 设计规范 → 从 Demo 抽象为规范文档

**Prompt**：
> "从 demo.html 提取完整的 UI 设计规范文档，包含配色表、字体层级、组件规格、间距系统"

**AI 输出**：[UI设计规范.md](UI设计规范.md) — CSS 变量表、排版层级、组件矩阵、响应式断点

**我验证了什么**：
- 所有 CSS 变量是否与 Demo 一致
- 组件状态是否覆盖了 hover/active/disabled

---

### 规划 → 编码：一句命令启动

```
规划全部完成后，我只说了一句话：
"从1开始，逐步推进"

AI 就按照开发计划.md 的 12 步顺序，一步一验收，逐步实现了整个项目。
```

---

## 第 10 页：优势、不足、自评分（必答题）

### 我们组的优势

| 优势 | 说明 |
|------|------|
| 🎯 **需求明确** | PRD + UI 规范 + 12 步开发计划，每个步骤有完成标准 |
| 🏗️ **架构完整** | 前端 SPA + 后端 Flask + 数据库 SQLite + 云端部署 |
| 🔄 **渐进式开发** | 严格按依赖关系分步推进，基础→功能→增强→部署 |
| 💬 **Prompt 规范** | 每步都带"目标+涉及文件+具体做什么+完成标准"四要素 |
| 🐛 **问题记录完善** | 循环导入/参数错误/依赖缺失等 7 个典型问题全记录 |

### 我们的不足

| 不足 | 说明 |
|------|------|
| 📱 **无移动端适配** | 仅做了 700px 响应式，没做手机端专门适配 |
| 🧪 **无自动化测试** | 完全依赖人工验证，没有单元测试 |
| 🔒 **安全简化** | bcrypt→SHA-256、PyJWT→HMAC，企业级场景不够安全 |
| 🛒 **无支付/邮箱** | 用户系统没有邮箱验证、密码找回等功能 |

### 自评分

> **评级：A 档**
>
> 理由：系统完整可用（6 页面 + 完整后端 + 云端部署）、遵循 Vibe Coding 三步要求（逻辑先行/渐进式/责任归属）、有详细的 Prompt 记录和 AI 纠错案例、小组分工明确。

---

## 第 11 页：答辩分工

| 环节 | 负责人 | 对应页码 | 内容 |
|------|:---:|:---:|------|
| 需求/设计文档 | **组员 A** | 第 2-3 页 | 需求背景 → 架构设计 |
| Vibe Coding 规划过程 | **组员 A** | 第 9 页 | 5 步规划（需求→PRD→计划→Demo→规范） |
| 系统演示 | **组员 B** | 第 5 页 | 6 个演示点全程操作展示 |
| Prompt + 纠错 | **组员 C** | 第 6-7 页 | 最棒 Prompt + 3 个 AI 犯错案例 |
| 反思 + 必答题 | **组员 A** | 第 8/10 页 | Vibe Coding 反思 + 优势/不足/自评 |
| 分工说明 | **组员 A** | 第 4 页 | 三人分工 + 各阶段参与度 |

> 注：原则上每人承担一个环节。组员 A 承担 3 个环节（开场+规划+总结），各控制在 1 分钟内。

---

## 附录：关键数据

| 指标 | 数值 |
|------|:---:|
| 项目文件总数 | 31 个 |
| 总代码行数 | ~3500 行 |
| 前端 JS 文件 | 15 个 |
| 后端 Python 文件 | 6 个 |
| 数据库表 | 3 张 |
| API 端点 | 14 个 |
| Git 提交次数 | 13 次 |
| AI Prompt 轮次 | ~50 轮 |
| 从零到上线耗时 | 1 天 |
| 外部依赖 | 仅 Flask + Chart.js CDN |